#include<bits/stdc++.h>
using namespace std;

//PAIRS    
        //declaration
    //pair<int,string> p;
        //initialisation 1st method
    //p = make_pair(2, "abc");
        //intialisation 2nd method
    //p ={2,"avb"};
    //cout<< p.first << " "<<p.second <<endl;
        //array 
   /* pair<int,int> p_array[3];
    p_array[0] = {1,2};
    p_array[1] = {2,3};
    p_array[2] = {3,4};
    swap(p_array[0],p_array[2]);
    for (int i = 0; i < 3; ++i)
    {
        cout<<p_array[i].first <<" " <<p_array[i].second<<endl;
    }*/

//VECTORS
   /*void printVector(vector<int> v){
    cout<< "size: "<< v.size() << endl;
    for (int i = 0; i < v.size(); ++i)
    {
        //v.size() -> O(1)
        cout<<v[i]<< " ";
    }
    }
   vector<int> v;
      int n;
      cin>>n;
      for (int i = 0; i < n; ++i)
      {
        int x;
        cin>>x;
        v.push_back(x); // O(1) it add at the last
        }
       printVector(v);
      */
        /*vector<int> v;
        v.push_back(7);
        v.push_back(6);
        v.pop_back(); // it remove from the last  O(1)
          //copy of v in v2
        vector<int> v2 = v; -> O(n) and v2 is the copy of the v vector, it means if we change smthng in v2
                              it does not reflect in v.
    */
    

    

